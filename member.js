function skillsMember()
{
    var skills = ["C", "C++", "Java", "Python"];
    var len = skills.length;
    var i = 0;
    // Accessing array elements using for loop
    for (i = 0; i < len; i++)
    {
        console.log("Skill: " + skills[i]);
    }
}